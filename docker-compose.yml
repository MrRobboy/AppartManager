version: '3'

services:
  backend:
    build:
      context: ./backend
      dockerfile: DockerFile
    ports:
      - "5000:5000"
  frontend:
    image: your-frontend-image
    ports:
      - "80:80"
